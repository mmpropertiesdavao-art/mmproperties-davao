import type {Metadata} from "next";
export const metadata:Metadata={title:"MM Pulse",description:"Smarter Davao property decisions for buyers, investors, and sellers. Match listings and explore indicative property value ranges with transparent local data."};
export default function Layout({children}:{children:React.ReactNode}){return children}
