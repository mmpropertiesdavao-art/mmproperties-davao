"use client";import{useEffect,useState}from"react";
export function DeveloperField({name="developerName",defaultValue="",className=""}:{name?:string;defaultValue?:string;className?:string}){const[developers,setDevelopers]=useState<{id:string;name:string}[]>([]);useEffect(()=>{fetch("/api/developers").then(r=>r.json()).then(setDevelopers).catch(()=>{})},[]);return <><input name={name} defaultValue={defaultValue} list="mm-developers" placeholder="Select or enter a developer" className={className}/><datalist id="mm-developers">{developers.map(d=><option key={d.id} value={d.name}/>)}</datalist></>}
