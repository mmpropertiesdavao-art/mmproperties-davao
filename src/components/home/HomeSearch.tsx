"use client";
import Link from "next/link";import{useState}from"react";import{Search,Sparkles}from"lucide-react";
export function HomeSearch(){const[intent,setIntent]=useState<"sale"|"rent">("sale");return <div className="mx-auto mt-8 max-w-3xl rounded-2xl bg-white/10 p-3 shadow-2xl backdrop-blur"><div className="mb-3 flex gap-2">{(["sale","rent"] as const).map(v=><button type="button" key={v} onClick={()=>setIntent(v)} className={`rounded-full px-4 py-2 text-sm font-semibold transition ${intent===v?"bg-gold-500 text-navy-950":"bg-white/10 text-white hover:bg-white/20"}`}>For {v}</button>)}</div><form action="/search" className="flex flex-col gap-2 sm:flex-row"><input type="hidden" name="listingIntent" value={intent}/><label className="sr-only" htmlFor="home-search">Search Davao properties</label><input id="home-search" name="q" placeholder="Barangay, neighborhood, subdivision, or property" className="min-w-0 flex-1 rounded-lg border-0 bg-white px-4 py-3 text-navy-900 shadow-sm placeholder:text-navy-400 focus:outline-none focus:ring-2 focus:ring-gold-400"/><button className="inline-flex items-center justify-center gap-2 rounded-lg bg-gold-500 px-6 py-3 font-semibold text-navy-950 hover:bg-gold-300"><Search size={18}/>Search</button></form><Link href="/matcher" className="mt-3 inline-flex items-center gap-2 text-sm font-semibold text-gold-300 hover:text-gold-100"><Sparkles size={16}/>Try MM Pulse for personalized matches</Link></div>}
